syntax = "proto3";

option go_package = "github.com/Hiroya3/grpc/first-grpc/grpc-define;grpc_define";

package grpc_define;

// 年齢を受け、生まれた年を返す
service BornYear{
  rpc GetBornYear (Age) returns (Year) {}
}

message Age{
  int32 age = 1;
}

message Year{
  int32 year = 1;
}